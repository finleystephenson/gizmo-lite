---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/, .gitignore, requirements.txt]
autonomous: true
---

<objective>
Establish project foundation with proper Python package structure and dependency management.

Purpose: Create organized codebase structure following Flask conventions so subsequent phases have a clear place to add code.
Output: src/ directory with Flask layout, .gitignore excluding virtualenv/cache, requirements.txt with core dependencies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/ directory structure</name>
  <files>src/__init__.py, src/app.py, src/models/__init__.py, src/routes/__init__.py</files>
  <action>Create src/ directory with Flask application layout. Create __init__.py files to make directories Python packages. Create empty src/app.py as Flask application entry point. Create src/models/ for database models and src/routes/ for API endpoints. Use standard Flask project structure suitable for a small application.</action>
  <verify>ls -la src/ shows all directories and __init__.py files exist</verify>
  <done>src/ structure exists with __init__.py in all subdirectories, app.py created</done>
</task>

<task type="auto">
  <name>Task 2: Create .gitignore</name>
  <files>.gitignore</files>
  <action>Create .gitignore with Python standard exclusions: .venv/ (virtual environment), __pycache__/ (bytecode cache), *.pyc (compiled Python), .env (secrets), .DS_Store (macOS), *.db (SQLite databases - local only). Use standard Python .gitignore template.</action>
  <verify>cat .gitignore shows all exclusions listed</verify>
  <done>.gitignore exists with Python/Flask standard exclusions</done>
</task>

<task type="auto">
  <name>Task 3: Create requirements.txt</name>
  <files>requirements.txt</files>
  <action>Create requirements.txt with core dependencies: Flask (web framework), python-dotenv (environment variables), anthropic (Claude API client). Use specific versions: Flask>=3.0.0, python-dotenv>=1.0.0, anthropic>=0.18.0. One dependency per line, no comments needed yet.</action>
  <verify>cat requirements.txt shows Flask, python-dotenv, anthropic listed</verify>
  <done>requirements.txt exists with Flask, python-dotenv, and anthropic dependencies</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] src/ directory structure exists with all subdirectories
- [ ] All __init__.py files present
- [ ] .gitignore excludes .venv/, __pycache__/, *.pyc, .env, *.db
- [ ] requirements.txt contains Flask, python-dotenv, anthropic
- [ ] No syntax errors or typos in any files
</verification>

<success_criteria>

- All tasks completed
- Project structure follows Flask conventions
- .gitignore prevents committing sensitive/generated files
- requirements.txt ready for pip install
- No errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
