---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/config.py, src/app.py, .env.example, README.md]
autonomous: true
---

<objective>
Configure Flask application with environment variables and provide setup documentation.

Purpose: Create runnable Flask app with proper configuration management and clear setup instructions for the student user.
Output: Flask app that initializes database on startup, environment template for API key, README with installation steps.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

# User context:
# - Student learning Flask + Tailwind + SQLite + Anthropic API
# - Wants step-by-step explanations in code and docs
# - Needs to obtain Anthropic API key (doesn't have one yet)
# - Local-first app (no cloud deployment required)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configuration module</name>
  <files>src/config.py</files>
  <action>Create config.py that loads environment variables using python-dotenv. Define Config class with: ANTHROPIC_API_KEY (from env, required for Phase 2), SECRET_KEY (for Flask sessions - generate random default), DATABASE_PATH (default to 'flashcards.db'). Include clear comments explaining each config variable's purpose since user is learning. Load from .env file using load_dotenv().</action>
  <verify>python -c "from src.config import Config; print(Config.DATABASE_PATH)" shows database path</verify>
  <done>Config class loads environment variables with clear documentation</done>
</task>

<task type="auto">
  <name>Task 2: Create Flask application skeleton</name>
  <files>src/app.py</files>
  <action>Create Flask app in app.py. Import Flask, load Config. Create app = Flask(__name__, template_folder='templates', static_folder='static'). Load config: app.config.from_object(Config). Initialize database on startup using init_db(). Add basic route @app.route('/') that returns "AI Flashcard Generator - Coming Soon" for now. Include if __name__ == '__main__': app.run(debug=True). Add comments explaining each section for learning purposes.</action>
  <verify>python src/app.py starts Flask dev server on localhost:5000</verify>
  <done>Flask app runs, initializes database, serves basic homepage</done>
</task>

<task type="auto">
  <name>Task 3: Create environment template</name>
  <files>.env.example</files>
  <action>Create .env.example with template for required environment variables. Include: ANTHROPIC_API_KEY=your_api_key_here (with comment explaining how to get it from console.anthropic.com). Include instructions comment at top: "Copy this file to .env and fill in your actual values. Never commit .env to git." Keep it simple and educational.</action>
  <verify>cat .env.example shows ANTHROPIC_API_KEY with placeholder and instructions</verify>
  <done>.env.example exists with clear instructions for API key setup</done>
</task>

<task type="auto">
  <name>Task 4: Create README with setup instructions</name>
  <files>README.md</files>
  <action>Create README.md with project description and detailed setup instructions. Sections: 1) Project description (AI flashcard generator for students), 2) Requirements (Python 3.12+), 3) Setup steps (create venv, pip install -r requirements.txt, copy .env.example to .env, add API key, run app), 4) How to get Anthropic API key (link to console.anthropic.com/settings/keys), 5) Project structure overview (src/models, src/routes), 6) Running the app (python src/app.py). Use clear step-by-step instructions since user is learning. Include helpful tips like "activate virtual environment first".</action>
  <verify>cat README.md shows complete setup instructions with all steps</verify>
  <done>README.md provides clear, beginner-friendly setup documentation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Flask app runs without errors (python src/app.py)
- [ ] Database initializes on app startup (flashcards.db created)
- [ ] .env.example has clear instructions for API key
- [ ] README.md has complete setup steps
- [ ] Config module loads environment variables correctly
- [ ] No hardcoded secrets (API key in .env only, not committed)
</verification>

<success_criteria>

- All tasks completed
- Flask app runs and serves basic homepage
- Database tables created automatically on first run
- Environment configuration ready for API key (Phase 2)
- README provides clear setup instructions for student user
- All files have helpful comments for learning
- No security issues (secrets in .env, not hardcoded)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
