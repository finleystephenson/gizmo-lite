---
phase: 07-export-import-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/templates/base.html, src/templates/index.html, src/templates/decks.html, src/templates/study.html, src/templates/stats.html, src/templates/preview.html]
autonomous: false
---

<objective>
Polish UI for consistency and mobile responsiveness.

Purpose: Ensure the app looks clean on both desktop and mobile devices with consistent styling.
Output: Responsive layouts, consistent button styles, proper spacing across all pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/templates/base.html
@src/templates/index.html
@src/templates/decks.html
@src/templates/study.html
@src/templates/stats.html
@src/templates/preview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport meta tag and ensure mobile base styling</name>
  <files>src/templates/base.html</files>
  <action>
1. Ensure viewport meta tag exists in head:
   <meta name="viewport" content="width=device-width, initial-scale=1.0">

2. Add responsive padding to main container:
   - Change px-4 to px-4 sm:px-6 lg:px-8 on container
   - Ensure max-w-4xl or max-w-6xl for content width

3. Make header responsive:
   - On mobile: stack logo and nav vertically OR use smaller text
   - Ensure nav links have enough tap target (min 44px height)
   - Add py-4 for proper vertical spacing
  </action>
  <verify>View site on mobile viewport (375px width in dev tools) - header should not overflow</verify>
  <done>Base template has viewport meta, responsive padding, and mobile-friendly header</done>
</task>

<task type="auto">
  <name>Task 2: Make forms and cards mobile responsive</name>
  <files>src/templates/index.html, src/templates/decks.html, src/templates/preview.html</files>
  <action>
1. index.html (homepage form):
   - Ensure textarea and inputs are w-full
   - Button should be full-width on mobile, auto on desktop: w-full sm:w-auto
   - Add proper vertical spacing (space-y-4)

2. decks.html:
   - Deck cards: flex-col on mobile, flex-row on desktop for button layout
   - Buttons stack vertically on mobile: flex-col sm:flex-row gap-2
   - Import form responsive: stack on mobile, inline on desktop

3. preview.html:
   - Card grid: single column on mobile, 2 columns on desktop
   - Use grid-cols-1 md:grid-cols-2 gap-4
   - Ensure card delete button is tappable size
  </action>
  <verify>View each page on mobile viewport - no horizontal scroll, buttons tappable</verify>
  <done>Forms and cards adapt to mobile with stacked layouts and proper tap targets</done>
</task>

<task type="auto">
  <name>Task 3: Make study mode and stats page mobile responsive</name>
  <files>src/templates/study.html, src/templates/stats.html</files>
  <action>
1. study.html:
   - Flashcard container: responsive width (w-full max-w-2xl)
   - Buttons: stack on mobile if needed, proper spacing
   - Summary modal: full-width on mobile with proper padding

2. stats.html:
   - Overall summary grid: 2 columns on mobile (grid-cols-2), 4 on desktop (md:grid-cols-4)
   - Statistics table: horizontal scroll wrapper for mobile
   - Add overflow-x-auto on table container
   - Ensure text doesn't wrap awkwardly
  </action>
  <verify>View study mode and stats on mobile - cards centered, table scrollable</verify>
  <done>Study mode and statistics are usable on mobile devices</done>
</task>

<task type="auto">
  <name>Task 4: Ensure consistent button styling across all pages</name>
  <files>src/templates/index.html, src/templates/decks.html, src/templates/preview.html, src/templates/study.html</files>
  <action>
Audit and standardize button styles across all pages:

1. Primary action buttons (Generate, Save, Study Now):
   - bg-blue-600 hover:bg-blue-700 text-white
   - py-2 px-6 (or py-3 px-6 for larger)
   - rounded-md font-bold
   - transition-colors

2. Success buttons (Got it!):
   - bg-green-600 hover:bg-green-700 text-white
   - Same padding and rounding

3. Danger buttons (Delete, Needs Practice):
   - bg-red-600 hover:bg-red-700 text-white
   - Same padding and rounding

4. Secondary/outline buttons (Export, Cancel):
   - border-2 border-blue-600 text-blue-600 hover:bg-blue-50
   - Same padding and rounding

Ensure all buttons have:
- Consistent padding (py-2 px-4 or py-2 px-6)
- transition-colors for smooth hover
- font-bold or font-semibold for readability
  </action>
  <verify>Visual inspection of all pages - buttons should look consistent</verify>
  <done>All buttons follow consistent styling patterns</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile responsive design and consistent UI styling across all pages</what-built>
  <how-to-verify>
    1. Run: cd ~/Expanza_Gary_Test && FLASK_APP=src.app flask run --port 5001
    2. Open Chrome DevTools (F12) → Toggle device toolbar (Ctrl+Shift+M)

    Desktop checks (1024px+):
    3. Visit http://localhost:5001 - form centered, button aligned
    4. Visit /decks - cards with buttons side by side
    5. Visit /stats - 4-column grid for summary stats
    6. Visit /study/{deck_id} - flashcard centered, buttons visible

    Mobile checks (375px - iPhone SE):
    7. Homepage: form fills width, button full-width
    8. /decks: buttons stack vertically, no horizontal scroll
    9. /stats: 2-column grid, table scrolls horizontally
    10. /study: card fills screen, buttons accessible
    11. /preview: single column cards, delete buttons tappable

    Consistency checks:
    12. All primary buttons are blue with same style
    13. All danger buttons are red with same style
    14. Flash messages appear correctly styled
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 7, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All pages work on mobile (375px) without horizontal scroll
- [ ] Buttons are consistent across all pages
- [ ] Tap targets are large enough (44px minimum)
- [ ] Statistics table scrolls horizontally on mobile
- [ ] Flash messages display correctly
- [ ] Human verification passed
</verification>

<success_criteria>

- All tasks completed
- UI is responsive on mobile devices
- Button styling is consistent
- User approved visual verification
- No CSS/layout issues introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-import-polish/07-02-SUMMARY.md`
</output>
